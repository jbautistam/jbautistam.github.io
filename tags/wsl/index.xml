<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>wsl on jbautistam</title><link>/tags/wsl/</link><description>Recent content in wsl on jbautistam</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><copyright>Copyright © 2008–2020, Jose Antonio Bautista Montejo; all rights reserved.</copyright><lastBuildDate>Sat, 10 Oct 2020 00:00:00 +0000</lastBuildDate><atom:link href="/tags/wsl/index.xml" rel="self" type="application/rss+xml"/><item><title>Instalación de Spark en local</title><link>/articles/big-data/instalacion-spark/instalacion-spark/</link><pubDate>Sat, 10 Oct 2020 00:00:00 +0000</pubDate><guid>/articles/big-data/instalacion-spark/instalacion-spark/</guid><description>
&lt;p>En estos últimos meses, me he tenido que enfrentar con mayor o menor éxito con &lt;a href="https://spark.apache.org/">Spark&lt;/a>.&lt;/p>
&lt;p>A muy alto nivel, Spark es un motor de proceso distribuido al que podemos mandar trabajos utilizando Python, Scala, R y SQL.&lt;/p>
&lt;p>Estos días he estado instalándolo sobre mi equipo con WSL y creo que es interesante detallar los pasos seguidos (sobre todo
para no olvidarme).&lt;/p>
&lt;p>En primer lugar, aunque se puede instalar directamente sobre Windows, me interesaba configurarlo sobre WSL más que nada por
no 'ensuciar' mi sistema. Si algo sale mal, simplemente puedo desinstalar el sistema Linux sin tener que hacer nada más. Supongo
que alguien lo considerará una herejía pero en este caso he utilizado WSL más como contenedor que como herramienta de trabajo.&lt;/p>
&lt;p>Lo primero que tenemos que hacer por tanto es instalar WSL, para ello vamos a &lt;strong>Configuración &amp;gt; Aplicaciones &amp;gt;
Programas y características&lt;/strong> y seleccionamos *&lt;em>Activar o desactivar las características de Windows&lt;/em>, donde debemos
activar, por supuesto, el &lt;strong>Subsistema de Windows para Linux&lt;/strong> (que no sé vosotros, pero opino que debería
llamarse al revés).&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Caracteristicas-de-Windows.jpg" alt="Activar o desactivar las características de Windows" title="Activar o desactivar las características de Windows">&lt;/p>
&lt;p>Una vez instalado (que si no me equivoco, necesita un reinicio)source-code/big-data/big-data/al Store de Windows y seleccionamos un sistema
Linux. Yo he elegido Ubuntu, más que nada por el instalador de paquetes, pero sentíos libres:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Ubuntu.jpg" alt="Ubuntu" title="Ubuntu">&lt;/p>
&lt;p>Cuando esté instalado, al ejecutarlo, nos pedirá el usuario y la clave del administrador y a partir de ese momento podemos trabajar
con el shell sin problemas.&lt;/p>
&lt;p>Supongo que es cosa de costumbre, pero particularmente prefiero trabajar con &lt;a href="https://cmder.net/">cmder&lt;/a> abriendo una consola de tipo WSL en lugar de directamente
con la propia consola de Ubuntu, más que nada por los comandos de copiar / pegar y cosas por el estilo pero supongo que es cuestión
de gustos.&lt;/p>
&lt;p>A partir de ahora, podemos ejecutar los comandos de instalación.&lt;/p>
&lt;p>Primero vamos a actualizar tanto el instalador de paquetes como el sistema:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>sudo apt update
&lt;span class="ln">2&lt;/span>sudo apt -y upgrade
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Y ahora vamos a instalar Java (recordemos que Spark está escrito en Scala, por tanto necesitamos el JVM):&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>sudo apt install default-jre
&lt;span class="ln">2&lt;/span>sudo apt install openjdk-11-jre-headless
&lt;span class="ln">3&lt;/span>sudo apt install openjdk-8-jre-headless
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Ahora ya podemos instalar Spark. Para ello vamos a seleccionar la versión desde la
&lt;a href="https://spark.apache.org/downloads.html">página de descargas&lt;/a>:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Download-spark.jpg" alt="Página de descargas de Spark" title="Página de descargas de Spark">&lt;/p>
&lt;p>Yo he elegido la versión 2.4.4 con Hadoop2.7 (spark-2.4.4-bin-hadoop2.7) así que vamos a descargarlo, descomprimirlo y dejarlo
en un directorio un poco más amigable:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>wget https://www-us.apache.org/dist/spark/spark-2.4.4/spark-2.4.4-bin-hadoop2.7.tgz
&lt;span class="ln">2&lt;/span>tar xvf spark-2.4.4-bin-hadoop2.7.tgz
&lt;span class="ln">3&lt;/span>sudo mv spark-2.4.4-bin-hadoop2.7/ /opt/spark
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Por si alguien se lo pregunta, Spark no necesita Hadoop instalado para funcionar, pero sí que necesita las clases dsource-code/big-data/big-data/eso es lo que hace referencia el nombre de archivo. Si queréis utilizar Hadoop lo tenéis que instalar aparte y en ese
caso sí podríais descargar la versión de Spark sin Hadoop en el nombre.&lt;/p>
&lt;p>Pues vamos a configurar Spark, para ello abrimos el archivo de configuración:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>nano ~/.bashrc
&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;strong>Nota:&lt;/strong> si tenéis instalado Visual Studio Code también podéis ejecutar code ~/.bashrc&lt;/p>
&lt;p>Al final de este archivo debemos insertar estas líneas:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>&lt;span class="nb">export&lt;/span> &lt;span class="nv">SPARK_HOME&lt;/span>&lt;span class="o">=&lt;/span>/opt/spark
&lt;span class="ln">2&lt;/span>&lt;span class="nb">export&lt;/span> &lt;span class="nv">PATH&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="nv">$PATH&lt;/span>:&lt;span class="nv">$SPARK_HOME&lt;/span>/bin:&lt;span class="nv">$SPARK_HOME&lt;/span>/sbin
&lt;span class="ln">3&lt;/span>&lt;span class="nb">export&lt;/span> &lt;span class="nv">JAVA_HOME&lt;/span>&lt;span class="o">=&lt;/span>&lt;span class="s2">&amp;#34;/usr/lib/jvm/java-8-openjdk-amd64&amp;#34;&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Las dos primeras configuran el directorio de Spark y lo añaden al entorno. La última configura la versión de Java, parece ser
que Spark 2.4 no se lleva bien con Java 11.&lt;/p>
&lt;p>Sólo nos queda refrescar la configuración:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>&lt;span class="nb">source&lt;/span> ~/.bashrc
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Y estamos ya preparados. Spark ya está instalado, sólo nos queda ponerlo en marcha.&lt;/p>
&lt;p>Primero vamos a arrancar un nodo master:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>start-master.sh
&lt;/code>&lt;/pre>&lt;/div>&lt;p>A partir de este momento podemos abrir un navegador para ver la interface de usuario sobre &lt;code>localhost:8080&lt;/code>:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Spark-UI-master.jpg" alt="Interface de usuario de master" title="Interface de usuario de master">&lt;/p>
&lt;p>Abrir el navegador (al menos la primera vez), es importante porque nos va a dar la dirección del nodo master que vamos
a necesitar a la hora de añadir nodos al cluster. En mi caso: &lt;code>spark://jbautistam-PC.localdomain:7077&lt;/code> que podéis ver
en la cabecera en negrita.&lt;/p>
&lt;p>Ahora ya podemos añadir un nodo de trabajo:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>start-slave.sh spark://dirección del nodo master:7077
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Por supuesto, este es un nodo que corre sobre mi máquina pero para añadir nodos en otras máquinas el proceso es el mismo.&lt;/p>
&lt;p>Y ya podemos ver el nodo worker en la UI:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Spark-UI-Worker.jpg" alt="Interface de usuario con un nodo master y otro worker" title="Interface de usuario con un nodo master y otro worker">&lt;/p>
&lt;p>Para el futuro, cuando queramos detener los nodos utilizaremos estas instrucciones:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>&lt;span class="nv">$SPARK_HOME&lt;/span>/sbin/stop-slave.sh
&lt;span class="ln">2&lt;/span>&lt;span class="nv">$SPARK_HOME&lt;/span>/sbin/stop-master.sh
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Un par de cosas más, podéis abrir la consola de Spark con:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>spark-shell
&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Spark-shell.jpg" alt="Shell de Spark" title="Shell de Spark">&lt;/p>
&lt;p>&lt;strong>Nota:&lt;/strong> para salir, escribid &lt;code>:quit&lt;/code> (de nada).&lt;/p>
&lt;p>Esta es la consola predefinida, con Scala, pero si sois más de Python, podéis instalar PySpark utilizando pip:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>sudo apt install python-pip
&lt;span class="ln">2&lt;/span>pip install pyspark
&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;strong>Nota:&lt;/strong> la primera instrucción es precisamente para instalar pip.&lt;/p>
&lt;p>Para ejecutar la consola de Python, simplemente ejecutad &lt;code>pyspark&lt;/code>:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Pyspark.jpg" alt="Pyspark" title="Pyspark">&lt;/p>
&lt;p>Por cierto, tenéis un directorio &lt;code>examples&lt;/code> con ejemplos de aplicaciones en Scala, Python y R y algunos archivos CSV, parquet,
Avro y json para pruebas.&lt;/p>
&lt;h2 id="conectando-a-spark-con-jdbc">Conectando a Spark con JDBC&lt;/h2>
&lt;p>Como hemos dicho, podemos acceder a Spark utilizando Psource-code/big-data/big-data/ y R, pero si sois como yo habréis escuchado hablar de
Spark Sql, por eso empezó todo precisamente.&lt;/p>
&lt;p>El caso es que la documentación de Spark es muy buena y puedes utilizar sin problemas SQL desde Python pero no hay una forma
sencilla de ejecutar sentencias SQL sin tener que definir dataframes, RDD, UDF y todas esas cosas extrañas que le gustan a la
gente que trabaja con datos.&lt;/p>
&lt;p>No dudo de su utilidad, pero yo lo único que quiero es conectame a Spark y lanzarle sentencias SQL y para ello tengo que dar un
par de pasos más.&lt;/p>
&lt;p>Concretamente, tenemos que lanzar el servidor JDBC / ODBC ejecutando la siguiente instrucción:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>&lt;span class="nv">$SPARK_HOME&lt;/span>/sbin/start-thriftserver.sh
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Un comentario sobre ésto, el resultado de este comando es algo así:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-bash" data-lang="bash">&lt;span class="ln">1&lt;/span>starting org.apache.spark.sql.hive.thriftserver.HiveThriftServer2,
&lt;span class="ln">2&lt;/span>logging to /opt/spark/logs/spark-jbautistam-org.apache.spark.sql.hive.thriftserver.HiveThriftServer2-1-jbautistam-PC.out
&lt;/code>&lt;/pre>&lt;/div>&lt;p>No da ningún error aunque no funcione (al menos a mí no me lo ha dado), los errores los encontraréis en el archivo de log. Tardé
un par de horas en encontrar que no levantaba el servicio porque la versión de Java estaba mal definida.&lt;/p>
&lt;p>El caso es que si todo ha ido bien, ya podéis entrar en la interface de usuario de Thrift en &lt;code>localhost:4040&lt;/code>:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Thrift-UI.jpg" alt="Interface de usuario Thrift" title="Interface de usuario Thrift">&lt;/p>
&lt;p>Que parece muy interesante pero en realidad no nos permite lanzar ninguna consulta, sólo ver el estado de los trabajos (y creedme,
la consultaréis a menudo).&lt;/p>
&lt;p>Siempre podemos utilizar la consola con &lt;code>spark-sql&lt;/code>:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Spark-sql.jpg" alt="Spark Sql" title="Spark Sql">&lt;/p>
&lt;p>Que francamente, acostumbrado al manager de SQL Server, tampoco me entusiasma. Pero ya que tenemos un servidor JDBC vamos a abrir
&lt;a href="https://dbeaver.io/">DBeaver&lt;/a> contra él.&lt;/p>
&lt;p>Si no lo habéis utilizado nunca, Dbeaver es un aplicación para administrar todo tipo de base de datos: Postgres,
MySql, Sql Server, SqLite y sobre todo (y por lo que nos interesa) Spark.&lt;/p>
&lt;p>Así que abrimos Dbeaver y creamos una conexión a Spark:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Conexion-dbeaver.jpg" alt="Conexión a Spark con Dbeaver" title="Conexión a Spark con Dbeaver">&lt;/p>
&lt;p>y la configuramos:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Configuracion-dbeaver.jpg" alt="Configuracion de la conexión con Dbeaver" title="Configuracion de la conexión con Dbeaver">&lt;/p>
&lt;p>En este caso, que voy a conectarme a mi equipo, no me hace falta usuario y contraseña pero por supuesto, se puede configurar
en el servidor.&lt;/p>
&lt;p>Cuando pulsemos sobre &lt;strong>Aceptar&lt;/strong>, la primera vez se descargará los drivers JDBC adecuados y nos abrirá la conexión a
nuestro servidor:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Dbeaver-con-Spark.jpg" alt="Dbeaver conectado a Spark" title="Dbeaver conectado a Spark">&lt;/p>
&lt;p>Ahora, ya podemos lanzar consultas SQL contra nuestro servidor Spark, por ejemplo, vamos a consultar con SQL uno de esos
archivos parquet de ejemplo de los que hablábamos hace un rato:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-sql" data-lang="sql">&lt;span class="ln">1&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="o">*&lt;/span>
&lt;span class="ln">2&lt;/span> &lt;span class="k">FROM&lt;/span> &lt;span class="n">parquet&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="o">`&lt;/span>&lt;span class="n">examples&lt;/span>&lt;span class="o">/&lt;/span>&lt;span class="n">src&lt;/span>&lt;span class="o">/&lt;/span>&lt;span class="n">main&lt;/span>&lt;span class="o">/&lt;/span>&lt;span class="n">resources&lt;/span>&lt;span class="o">/&lt;/span>&lt;span class="n">users&lt;/span>&lt;span class="p">.&lt;/span>&lt;span class="n">parquet&lt;/span>&lt;span class="o">`&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>y vemos el resultado:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/dbeaver-query.jpg" alt="Resultado consulta con Dbeaver" title="Resultado consulta con Dbeaver">&lt;/p>
&lt;p>También podemos ir a la consola de Thrift para ver cómo se ha ejecutado todo:&lt;/p>
&lt;p>&lt;img src="/articles/big-data/instalacion-spark/images/Thrift-jobs.jpg" alt="Información sobre procesos de Thrift" title="Información sobre procesos de Thrift">&lt;/p>
&lt;p>&lt;strong>Nota:&lt;/strong> la documentación de Spark Sql en la propia web de Spark es bastante reducida,
simplemente nos redirigen a la de Hive pero para mi gusto la de
&lt;a href="https://docs.databricks.com/spark/latest/spark-sql/index.html#sql-language-manual">Databricks&lt;/a>
está bastante más cuidada.&lt;/p>
&lt;p>Pues eso es todo, en poco más de 20 minutos tenemos un entorno de Spark montado al que podemos acceder no sólo con
Python y Scala si no también con SQL.&lt;/p></description></item><item><title>Intérprete de scripts SQL</title><link>/articles/big-data/interprete-scripts-spark/interprete-scripts-spark/</link><pubDate>Sat, 10 Oct 2020 00:00:00 +0000</pubDate><guid>/articles/big-data/interprete-scripts-spark/interprete-scripts-spark/</guid><description>
&lt;p>Siempre he defendido SQL como un lenguaje ideal para el tratamiento de datos con una salvedad: la falta de instrucciones
imperativas en el estándar.&lt;/p>
&lt;p>SQL es un lenguaje declarativo, de ahí viene realmente su potencia: ejecutar una instrucción indicando lo que deseo obtener no
cómo se va a obtener.&lt;/p>
&lt;p>Funciona aunque a los programadores, acostumbrados a lenguajes imperativos, se nos suele dar bastante mal cuando nos quitan &lt;code>for&lt;/code>,
&lt;code>if&lt;/code> y similares. Una vez te das cuenta que para trabajar con SQL debes olvidar los bucles y pensar en bloques de datos, todo va rodado.&lt;/p>
&lt;p>Desgraciadamente, en ocasiones necesitamos instrucciones como &lt;code>if&lt;/code>, &lt;code>while&lt;/code>, declaraciones de variables, etc...
Hay formas de hacerlo utilizando SQL puro pero suelen ser bastante complicadas, por ejemplo, ¿quién sabría
decirme qué hace esta instrucción escrita en Spark Sql?&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-sql" data-lang="sql">&lt;span class="ln">1&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="n">date_add&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="nb">Date&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="n">CONCAT&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="k">CAST&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="k">Year&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="k">Current_Date&lt;/span>&lt;span class="p">())&lt;/span> &lt;span class="o">-&lt;/span> &lt;span class="mi">5&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">string&lt;/span>&lt;span class="p">),&lt;/span> &lt;span class="s1">&amp;#39;-01-01&amp;#39;&lt;/span>&lt;span class="p">)),&lt;/span> &lt;span class="n">Row_Number&lt;/span>&lt;span class="p">()&lt;/span> &lt;span class="n">OVER&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">ORDER&lt;/span> &lt;span class="k">BY&lt;/span> &lt;span class="n">Counter&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="o">-&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="nb">Date&lt;/span>
&lt;span class="ln">2&lt;/span> &lt;span class="k">FROM&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">Counter&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">tmp1&lt;/span>
&lt;span class="ln">3&lt;/span> &lt;span class="k">CROSS&lt;/span> &lt;span class="k">JOIN&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">tmp2&lt;/span>
&lt;span class="ln">4&lt;/span> &lt;span class="k">CROSS&lt;/span> &lt;span class="k">JOIN&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">tmp3&lt;/span>
&lt;span class="ln">5&lt;/span> &lt;span class="k">CROSS&lt;/span> &lt;span class="k">JOIN&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">tmp4&lt;/span>
&lt;span class="ln">6&lt;/span> &lt;span class="k">CROSS&lt;/span> &lt;span class="k">JOIN&lt;/span> &lt;span class="p">(&lt;/span>&lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span> &lt;span class="k">UNION&lt;/span> &lt;span class="k">ALL&lt;/span> &lt;span class="k">SELECT&lt;/span> &lt;span class="mi">1&lt;/span>&lt;span class="p">)&lt;/span> &lt;span class="k">AS&lt;/span> &lt;span class="n">tmp5&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Aunque no lo parezca, es sencillo: está obteniendo los días entre el uno de enero de hace cinco años hasta una fecha indeterminada (tendría
que ponerme a contar, lo siento). Es decir, esa serie de &lt;code>SELECT / CROSS JOIN / UNION ALL&lt;/code>, junto con la función de ventana
&lt;code>Row_Number&lt;/code> sirven para definir un contador de 1 a n (de nuevo, me tendría que poner a contar).&lt;/p>
&lt;p>Pero que se pueda, no implica que sea fácil, por eso la mayoría de los gestores de bases de datos relacionales avanzados incorporan
un lenguaje imperativo: SQL Server tiene TSql, Oracle tiene su PL/SQL, etc...&lt;/p>
&lt;p>Spark lo soluciona de una forma diferente, además de escribir sentencias en Spark Sql, podemos embeber Spark Sql en scripts de Python, la
combinación perfecta. Siempre que sepas Python, claro. No es nuevo, es lo que hacemos en la mayoría de los lenguajes cuando lanzamos
una sentencia SQL contra una conexión a base de datos.&lt;/p>
&lt;p>Pero dejémonos de introducciones ¿a qué viene todo ésto? Pues todo esto viene a que mientras programo
&lt;a href="/applications/baudbstudio/manual/001-tutorial-baudbstudio/001-tutorial-baudbstudio">BauDbStudio&lt;/a>, cuánto más ejecuto contra Spark Sql, más me
doy cuenta que necesito un lenguaje imperativo. Echo de menos los &lt;code>if&lt;/code>, &lt;code>for&lt;/code>, &lt;code>while&lt;/code> y demás. No siempre, pero cuando
hace falta, tengo que reconocer que cuesta horrores pasarlo por alto.&lt;/p>
&lt;p>Podría haber elegido la opción de ejecutar Python en &lt;strong>BauDbStudio&lt;/strong>, hay librerías para hacerlo pero ¿qué ocurriría con
otros gestores de bases de datos como SqLite o Postgress?, no me acababa de convencer (aunque no descarto acabar haciéndolo).&lt;/p>
&lt;p>La otra forma era definir mi propio lenguaje imperativo e interpretarlo desde &lt;strong>BauDbStudio&lt;/strong>. Esa fue la opción que elegí, por cacharrear, ya sabéis.&lt;/p>
&lt;p>Aún está en fase de pruebas, pero ya podemos escribir scripts como éste:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="ln"> 1&lt;/span>declare int valor = 20;
&lt;span class="ln"> 2&lt;/span>
&lt;span class="ln"> 3&lt;/span>if valor &amp;gt; 10
&lt;span class="ln"> 4&lt;/span>{
&lt;span class="ln"> 5&lt;/span> &amp;lt;%
&lt;span class="ln"> 6&lt;/span> DROP TABLE IF EXISTS {{DbCompute}}.TestScript
&lt;span class="ln"> 7&lt;/span> GO
&lt;span class="ln"> 8&lt;/span>
&lt;span class="ln"> 9&lt;/span> CREATE TABLE {{DbCompute}}.TestScript AS
&lt;span class="ln">10&lt;/span> SELECT *
&lt;span class="ln">11&lt;/span> FROM {{DbCompute}}.Ventas
&lt;span class="ln">12&lt;/span> WHERE Cantidad &amp;gt; #valor
&lt;span class="ln">13&lt;/span> GO
&lt;span class="ln">14&lt;/span> %&amp;gt;
&lt;span class="ln">15&lt;/span>}
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Como se puede ver, es una mezcla entre los &lt;a href="/applications/baudbstudio/manual/040-scripts-sql/040-scripts-sql">scripts de SQL&lt;/a> que teníamos
hasta ahora en &lt;strong>BauDbStudio&lt;/strong> con instrucciones SQL separadas por comandos &lt;code>GO&lt;/code>.&lt;/p>
&lt;p>Por el momento el lenguaje permite lo básico:&lt;/p>
&lt;ul>
&lt;li>Declaración de variables tipadas&lt;/li>
&lt;li>Cálculo de expresiones&lt;/li>
&lt;li>Definición de funciones&lt;/li>
&lt;li>Instrucciones condicionales (if, else)&lt;/li>
&lt;li>Instrucciones iterativas (for, while, do... while)&lt;/li>
&lt;li>Llamada a funciones&lt;/li>
&lt;li>Declaración de funciones predefinidas, es decir, aquellas que va a ejecutar el programa principal, no el intérprete (por ejemplo, print, dateAdd, ...)&lt;/li>
&lt;li>Ejecución de sentencias SQL&lt;/li>
&lt;/ul>
&lt;p>Suficiente para tener un lenguaje iterativo con SQL embebido para, en teoría, cualquier gestor de base de datos integrado en
&lt;strong>BauDbStudio&lt;/strong> (en este momento, SqLite, SQL Server, PostgreSql, MySql y, por supuesto, Spark).&lt;/p>
&lt;p>Estoy en fase de pruebas y moderadamente esperanzado. En un mes (modificando otros intérpretes que había desarrollado en el pasado), el
código ha avanzado mucho, ejecuta correctamente aunque le falta pulir algunos detalles como el paso de parámetros y cosas por
el estilo que aún dan algo de guerra.&lt;/p>
&lt;p>Digamos que por ahora, el lenguaje ha quedado un poco &lt;em>primitivo&lt;/em>, tenemos sentencias &lt;code>declare&lt;/code> o &lt;code>let&lt;/code> o &lt;code>call&lt;/code> en lugar de inferir
que estamos en una declaración de tipos o una variable o una llamada a función (ahora interpreta el código a partir de palabras reservadas).
También obliga a que las sentencias iterativas y condicionales (&lt;code>if&lt;/code>, &lt;code>while&lt;/code>, ...) estén dentro de un bloque de sentencias (con llaves) aunque
sólo tengan una sentencia. Supongo que el lenguaje evolucionará, pero por ahora era la forma más sencilla.&lt;/p>
&lt;h2 id="dónde-nos-deja-esto-con-databricks">¿Dónde nos deja esto con &lt;strong>DataBricks&lt;/strong>?&lt;/h2>
&lt;p>Actualmente puedo ejecutar en local las instrucciones de mi programa sin problema pero esto jamás va a poder subir a &lt;strong>Databricks&lt;/strong>. Es
imposible que mi nuevo lenguaje se ejecute en ese entorno.&lt;/p>
&lt;p>Podríamos hacer una cosa: reproducir el intérprete en Python de forma que recogiera el script y lo interpretara en tiempo de ejecución en
Databricks. Por supuesto no lo voy a hacer, entre otras cosas porque Python no es mi fuerte. También lo podríamos hacer utilizando
&lt;a href="/articles/big-data/experimento-spark-net/experimento-spark-net">Spark.Net&lt;/a> con C# pero no creo que por hoy por hoy tenga los conocimientos
suficientes como para meterme en ese sembrao.&lt;/p>
&lt;p>La segunda opción (que me parece la mejor) es transpilar.&lt;/p>
&lt;p>Como sabemos, la primera fase de la interpretación es el análisis sintáctico que devuelve una colección de sentencias y
expresiones en un lenguaje interno. La idea es transpilar este grupo de sentencias a un notebook de Python para databricks
con Spark Sql inmerso. Algo así:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-Python" data-lang="Python">&lt;span class="ln"> 1&lt;/span>&lt;span class="n">valor&lt;/span> &lt;span class="o">=&lt;/span> &lt;span class="mi">20&lt;/span>
&lt;span class="ln"> 2&lt;/span>
&lt;span class="ln"> 3&lt;/span>&lt;span class="k">if&lt;/span> &lt;span class="n">valor&lt;/span> &lt;span class="o">&amp;gt;&lt;/span> &lt;span class="mi">10&lt;/span>&lt;span class="p">:&lt;/span>
&lt;span class="ln"> 4&lt;/span> &lt;span class="n">spark&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">Sql&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s2">&amp;#34;DROP TABLE IF EXISTS DbCompute.TestScript&amp;#34;&lt;/span>&lt;span class="p">)&lt;/span>
&lt;span class="ln"> 5&lt;/span> &lt;span class="n">spark&lt;/span>&lt;span class="o">.&lt;/span>&lt;span class="n">Sql&lt;/span>&lt;span class="p">(&lt;/span>&lt;span class="s1">&amp;#39;&amp;#39;&amp;#39;CREATE TABLE {{DbCompute}}.TestScript AS
&lt;/span>&lt;span class="ln"> 6&lt;/span>&lt;span class="s1"> SELECT *
&lt;/span>&lt;span class="ln"> 7&lt;/span>&lt;span class="s1"> FROM {{DbCompute}}.Ventas
&lt;/span>&lt;span class="ln"> 8&lt;/span>&lt;span class="s1"> WHERE Cantidad &amp;gt; {}
&lt;/span>&lt;span class="ln"> 9&lt;/span>&lt;span class="s1"> &amp;#39;&amp;#39;&amp;#39;&lt;/span>&lt;span class="p">,&lt;/span>
&lt;span class="ln">10&lt;/span> &lt;span class="n">valor&lt;/span>&lt;span class="p">)&lt;/span>
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Ni que decir tiene que eso todavía ni lo he empezado pero la teoría es que debería funcionar.&lt;/p>
&lt;h2 id="pruebas-unitarias">Pruebas unitarias&lt;/h2>
&lt;p>Y una pregunta lanzada al viento ¿cómo se crean pruebas unitarias para un intérprete?&lt;/p>
&lt;p>La teoría es que a partir de un script de entrada debemos obtener una lista de sentencias que vienen del analizador léxico y sintáctico.
Podemos comprobar que esas sentencias sean las que esperamos, pero así estaríamos comprobando los analizadores, no el intérprete.&lt;/p>
&lt;p>¿Cómo probamos entonces que la ejecución de un programa interpretado pasa por todos los puntos y obtiene los resultados correctos?&lt;/p>
&lt;p>Pues no me parece fácil. En mi caso lo he solucionado aprovechando la forma en que está concebido el intérprete.&lt;/p>
&lt;p>El intérprete en realidad no sabe nada de SQL. Por dos razones: en primer lugar porque el SQL de cada gestor de base de datos es
ligeramente diferente (los estándares, ya sabéis), por tanto escribir un intérprete común es prácticamente imposible.
El intérprete sólo reconocer cadenas de comandos SQL (las que van entre &lt;code>&amp;lt;%&lt;/code> y &lt;code>%&lt;/code>&amp;gt;) y lanza una petición al procesador principal.&lt;/p>
&lt;p>Lo mismo ocurre con las funciones intrínsecas como &lt;code>print&lt;/code>, simplemente lanza una llamada al procesador.&lt;/p>
&lt;p>Por tanto, en este código:&lt;/p>
&lt;div class="highlight">&lt;pre class="chroma">&lt;code class="language-fallback" data-lang="fallback">&lt;span class="ln"> 1&lt;/span>declare int valor = 20;
&lt;span class="ln"> 2&lt;/span>
&lt;span class="ln"> 3&lt;/span>if valor &amp;gt; 10
&lt;span class="ln"> 4&lt;/span>{
&lt;span class="ln"> 5&lt;/span> &amp;lt;%
&lt;span class="ln"> 6&lt;/span> DROP TABLE IF EXISTS {{DbCompute}}.TestScript
&lt;span class="ln"> 7&lt;/span> GO
&lt;span class="ln"> 8&lt;/span>
&lt;span class="ln"> 9&lt;/span> CREATE TABLE {{DbCompute}}.TestScript AS
&lt;span class="ln">10&lt;/span> SELECT *
&lt;span class="ln">11&lt;/span> FROM {{DbCompute}}.Ventas
&lt;span class="ln">12&lt;/span> WHERE Cantidad &amp;gt; #valor
&lt;span class="ln">13&lt;/span> GO
&lt;span class="ln">14&lt;/span> %&amp;gt;
&lt;span class="ln">15&lt;/span>}
&lt;span class="ln">16&lt;/span>
&lt;span class="ln">17&lt;/span>call print(&amp;#34;valor: &amp;#34; + valor);
&lt;/code>&lt;/pre>&lt;/div>&lt;p>Se va a llamar al procesador principal dos veces: la primera con el comando SQL, la segunda con un mensaje a imprimir.&lt;/p>
&lt;p>Pero ¿quién es este procesador principal? En mi caso, cualquier clase que cumpla con una &lt;strong>interface&lt;/strong> con, por ahora, dos métodos:
ejecutar SQL y ejecutar función.&lt;/p>
&lt;p>Así que para hacer las pruebas unitarias, simplemente tengo que definir una clase que cumpla con la interface y vaya almacenando las
sentencias lanzadas con sus parámetros y compararlas con un resultado.&lt;/p>
&lt;p>Por ahora, mis pruebas unitarias consisten en métodos que recogen dos archivos: el script de entrada y los resultados previstos.&lt;/p>
&lt;p>Para hacer la prueba, simplemente se carga el script, se ejecuta y se comprueba que los resultados ejecutados sean los mismos que
los resultados previstos.&lt;/p>
&lt;p>En este momento aún es sencillo, los scripts de pruebas son bastante pequeños pero no sé si evolucionará adecuadamente para
scripts más grandes o si necesito probar realmente scripts más grandes o sólo pequeños trozos de programas.&lt;/p>
&lt;p>¿Alguna idea sobre esta cuestión en particular?&lt;/p>
&lt;h2 id="final">Final&lt;/h2>
&lt;p>Pues gracias por llegar hasta aquí.&lt;/p>
&lt;p>Aún estoy trabajando en ello, supongo que estos quince días pasará por todas las pruebas necesarias, corregiré los flecos y comenzaré
el transpilador.&lt;/p>
&lt;p>Hasta entonces, perdonadme que aún no muestre el código, de alguna forma desconocida he acabado con tres intérpretes:
uno para sentencias SQL normales, otro para ETLs y otro para scripts del nuevo lenguaje (que por ahora he llamado SQLx). Un poco de
limpieza no le va a venir mal antes de salir al público.&lt;/p></description></item></channel></rss>